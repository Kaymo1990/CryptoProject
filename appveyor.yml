version: '{build}'
skip_tags: true
clone_depth: 10
branches:
  only:
    - master
  except:
    - gh-pages
os: Windows Server 2012
install:
  - cmd: SET PATH=C:\Ruby23-x64\bin;%PATH%
  - cmd: ruby 2.6.5
  # - cmd: git --version
build_script:
  - bundle update
  - bundle install